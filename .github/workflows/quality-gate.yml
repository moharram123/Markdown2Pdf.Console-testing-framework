name: quality-gate

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore local tools
        run: dotnet tool restore

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create result folders
        run: |
          mkdir -p results/generated-pdfs
          mkdir -p results/test-reports
          mkdir -p results/metrics
          mkdir -p results/diffs
          mkdir -p results/llm-generated-tests

      # NOTE: The LLM test generator is now integrated into pytest via
      # test_llm_test_generation_runs() and test_llm_vs_manual_comparison()
      # in llm_test_generator.py, so it appears in the HTML test report.
      # No separate script call is needed here.
      - name: Run tests (includes LLM generation step)
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          pytest tests --html=results/test-reports/report.html --self-contained-html

      - name: Debug results folder
        if: always()
        run: |
          echo "=== RESULTS TREE ==="
          find results -maxdepth 3 -type f | sort

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quality-gate-artifacts
          path: results/

      - name: Commit results back to repo
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --soft origin/main
          git add results/
          git diff --staged --quiet || git commit -m "ci: update test results [skip actions]"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
